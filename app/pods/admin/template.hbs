<h2>Users Admin</h2>
<table>
  {{#each user in content}}
    <tr>
      <td>{{user.username}}</td>
      <td>{{user.email}}</td>
      <td>
        {{#if user.activated}} Active {{else}}[enable button here] {{/if}}
      </td>
    </tr>
  {{/each}}
</table>
