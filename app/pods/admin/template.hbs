<h2>Users Admin</h2>
<table>
  {{#each user in content}}
    <tr>
      <td>{{user.username}}</td>
      <td>
        <button {{action "enableUser" user}}>Enable</button>
      </td>
    </tr>
  {{/each}}
</table>
